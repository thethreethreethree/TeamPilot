# REMEDIATE

### `text-amber-300` on the error line

gate-or-promise: promise

Fifth file today, same value, same fix. Still not gated, and the reason is now settled rather than
deferred: **a gate for this class must know which ground it is standing on, and the class name does
not carry it.** The roleplay composer proved the stronger form — there the white-alpha value is
correct and the theme token is the defect, so a mechanical rewrite would have made the worst screen
of the day worse.

What CAN be gated, and is now the standing proposal rather than a fourth deferral of `paleText`:
widen `theme-audit.mjs`'s property list. Every sweep run today matched `border|bg|text`, which is
why `DoorDial`'s `stroke-white/15` survived all of them and was found by looking at a picture. The
full list is `border | bg | text | stroke | fill | divide | ring | from | to | via`.

### A file that is a class member with no instance of the class

gate-or-promise: gate

**Already gated, in the previous build, by fixing the producer.** This build is the proof that it
worked: four cards and a pill on this page render correctly on cream and this file was not touched.

The promise that survives it: **when a class is found on two surfaces, look for the producer before
fixing the third instance.** Today that took until the twelfth.

### The error state is a dead end

gate-or-promise: declined — proposed to the founder instead

Not a gate and not a promise: a behaviour change, outside the scope of "render every screen and
look", raised the day of an investor demo. It is in the residual with its evidence so the founder
can call it.

Recorded here because of what makes it interesting rather than what makes it severe: the clause it
violates is cited **in this same file, thirty lines away**, applied to the neighbouring state. That
is the eleventh time today the knowledge needed to avoid a defect was already present and close —
`pitch-score/route.ts:21`, `recover-transcripts-cron`, `outcomeLabels.ts`, `globals.css:120`, the
Analytics ternary, the catch-all comment written directly above the catch-all, `data?.scores?.[d]`
two lines above `data?.kpi.`, `PitchBreakdown`'s two guarded fields beside its unguarded one, and
now this.

None of it is carelessness. It is what happens when a decision is made correctly in one place and
the next person — usually the same person, often the same hour — makes it again somewhere else
without looking up.
