# THINK — the screen the correction named

started_at: 2026-09-24T09:36:00Z

## Why this one, and why now

Forty minutes ago I reported the render pass complete at "thirteen of thirteen". The module has 23
routes. `docs/tbc/2026-09-24-thirteenth-surface-one-liners/CORRECTION.md` records that, and named
the single most consequential of the ten unrendered routes:

**`/[id]/after-pitch` — the After Pitch Summary.** 1815 lines, ten white-alpha sites, and the
screen this product's own copy calls the real output. One Liners says it in words: *"Your real
calls get the full timeline and score in the After Pitch Summary."*

It is also the natural end of the manager demo path — Coach Assessment → a rep → a recording — so
on the founder's chosen route it is the LAST screen an investor sees, and it was the screen I could
say least about.

A correction that names a gap and does not close it is a better-written version of the same
failure. This closes it.

## Hypotheses before looking

1. The seven `border-white/[0.07] bg-white/[0.02]` sites are container cards and will be borderless
   on cream — the same class as the deck kit, hand-rolled here rather than imported.
2. The three remaining sites (`bg-white/12` ×2, `bg-white/25`) are at lines 1075-1086, inside
   `Timeline` — a connecting track and the non-breakdown nodes. If so, the timeline on cream is one
   ember dot with nothing around it.
3. Thirteen raw `-300`/`-400` text tints will be pale, as they were on four files before this.

## The thing the hypotheses did not anticipate

This page has **two branches**, and they are two different screens. `isStandard` at :715 splits
them. **Standard renders no timeline at all** — scores, read, cue loop, focus. The timeline, the
breakdown moment and its correct line are EXPERT-only.

So a Standard-only capture would have photographed this page, reported it rendered, and left every
timeline value unseen — which is the same failure as the one this build exists to correct, one
level down. A capture of a branch is not a capture of a file.
