import { SalesCoachComing } from "@/components/sales-coach/SalesCoachComing";

export default function SalesCoachTeamPage() {
  return (
    <SalesCoachComing
      title="Team"
      whatItIs="Manage who uses the Sales Coach and whether they're staff or an admin (the sales_coach_role added in this phase). Admins will add/remove members and see team-level coaching health here. Coming as a dedicated build."
    />
  );
}
