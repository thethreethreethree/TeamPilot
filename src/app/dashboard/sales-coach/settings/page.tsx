import { SalesCoachComing } from "@/components/sales-coach/SalesCoachComing";

export default function SalesCoachSettingsPage() {
  return (
    <SalesCoachComing
      title="Settings"
      whatItIs="Settings for both admins and staff — admins configure the coaching methodology corpus, the voice, and product-wide options; staff manage their own account and preferences. Coming as a dedicated build."
    />
  );
}
